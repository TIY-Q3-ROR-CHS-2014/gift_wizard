%h1 Listing groups

%table.groups
  %tr
    %th Name
    %th Email
    %th
    %th
    %th
    %th Secret Santa Name

  - @groups.each do |group_var|
    = render 'groups/group', group: group_var

= form_for @group, remote: true do |f|
  = f.label :name
  = f.text_field :name
  %br
  = f.label :email
  = f.text_field :email
  %br
  = f.submit

%br

= link_to 'New Group', new_group_path
